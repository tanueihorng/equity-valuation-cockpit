# OPTIONAL add-on — NOT needed for the core cockpit (HTML is zero-dependency;
# the Excel only needs openpyxl). Install only if you want TimesFM forecasting.
timesfm[torch]>=2.5
numpy>=1.24
pandas>=2.0        # only for --csv input
